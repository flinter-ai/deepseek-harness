/**
 * The AWS worker profile's runtime content is its public Cordis patch.
 * DSH owns the actual profile loader and harness; this module is intentionally
 * empty so installing the bundle does not create another runtime.
 */

export {}
